# Production build defaults. Both values are public (the GA4 measurement id
# appears in every served page), so this file is committed on purpose.
# Secrets — e.g. NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION — belong in the Vercel
# env, never here.
NEXT_PUBLIC_SITE_URL=https://opscrunch.com
NEXT_PUBLIC_GA4_ID=G-7XG10CD70E
